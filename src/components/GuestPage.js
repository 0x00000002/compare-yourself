import React from 'react'
import Header from './Header'

const GuestPage = () => (
  <div>
    <Header />
    <main>
      <h1>Main page</h1>
      All good, but you are not logged in.
    </main>
  </div>
)

export default GuestPage
