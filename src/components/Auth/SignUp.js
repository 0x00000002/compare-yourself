import React from 'react'

const signUp = () => { document.title = 'Sign Up' }

const SignUp = () => {
  return (
    <button onClick={signUp}>
      SignUp
    </button>
  )
}

export default SignUp
